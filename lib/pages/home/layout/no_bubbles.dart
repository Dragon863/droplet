import 'package:flutter/material.dart';

class NoBubblesContent extends StatelessWidget {
  const NoBubblesContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
